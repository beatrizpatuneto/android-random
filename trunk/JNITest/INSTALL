1. Get and setup toolchain:

  http://benno.id.au/blog/2007/11/13/android-native-apps

2. Build the lib/ dir with the supplied Makefile.
3. Install libNativeMD5.so onto the emulator:

  adb push libNativeMD5.so /system/lib

4. Build Android test project.
5. Install onto the emulator:

  adb install bin/JNITest.apk
 
Run JNITest on Android.  Check `adb logcat' for output.
